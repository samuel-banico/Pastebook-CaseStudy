import { InputLengthDirective } from './input-length.directive';

describe('InputLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLengthDirective();
    expect(directive).toBeTruthy();
  });
});
